// => Arrow Function :-
// It is called tha fat arrow (Because -> is a thin arrow & => is a fat arrow).
// and also called lamda function (because of otther languages).

let renishBhuva = () => {
    console.log("Subscribe to my channel");
}
renishBhuva();